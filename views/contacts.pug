//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Custom Object Table

        p
            a(href="/update") Go to Update Page
        table
            thead
                tr
                    th Name
                    th Genre
                    th Rating
            tbody
                each movie in data
                    tr
                        td #{movie.properties.movie_name}
                        td #{movie.properties.movie_genre} 
                        td #{movie.properties.movie_rating}
                
        